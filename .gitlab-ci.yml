stages:          # Liste des étapes pour les jobs
  - build

build-job:       # Ce job s'exécute dans l'étape de build
  stage: build
  image: maven:3.9.3-openjdk-17  # Utilise une image Docker Maven 3.9.3 avec JDK 17
  script:
    - mvn clean install      # Exécute la commande Maven
